{
  "name": "memory-parlour",
  "version": "0.1.0",
  "private": true,
  "description": "Memory Parlour — single-page fold site (Home fold build).",
  "scripts": {
    "dev": "node server/dev-server.js",
    "start": "node server/dev-server.js",
    "test:r2": "node server/r2.test.js"
  }
}
